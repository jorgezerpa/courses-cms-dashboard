import { useRouter } from 'next/router'
import React, { useState } from 'react'
import { ConfirmDeleteModal } from './ConfirmDeleteModal'

export const ItemOptions = ({id}:{id:number|string}) => {
    const router = useRouter()


    return(
<div></div>
    )
  }